CREATE TABLE [dbo].[c_Age_Range_Assessment] (
    [list_id]       VARCHAR (24) NOT NULL,
    [age_range_id]  INT          NOT NULL,
    [assessment_id] VARCHAR (24) NOT NULL,
    [sort_sequence] SMALLINT     NULL
);

