CREATE TABLE [dbo].[c_External_Observation] (
    [external_source]      VARCHAR (24) NOT NULL,
    [external_observation] VARCHAR (64) NOT NULL,
    [description]          VARCHAR (80) NULL,
    [observation_id]       VARCHAR (24) NULL
);

