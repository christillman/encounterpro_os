CREATE TABLE [dbo].[c_Message_Stream] (
    [message_type]          VARCHAR (24) NOT NULL,
    [stream_id]             VARCHAR (24) NOT NULL,
    [streamer_component_id] VARCHAR (24) NOT NULL
);



