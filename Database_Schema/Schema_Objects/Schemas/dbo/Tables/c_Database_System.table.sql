CREATE TABLE [dbo].[c_Database_System] (
    [system_id]         VARCHAR (24) NOT NULL,
    [system_type]       VARCHAR (12) NOT NULL,
    [current_version]   VARCHAR (24) NOT NULL,
    [available_version] VARCHAR (24) NOT NULL,
    [sort_sequence]     INT          NOT NULL
);



