CREATE TABLE [dbo].[c_Specialty_Observation_Category] (
    [specialty_id]            VARCHAR (24) NOT NULL,
    [treatment_type]          VARCHAR (24) NOT NULL,
    [observation_category_id] VARCHAR (24) NOT NULL,
    [sort_sequence]           SMALLINT     NULL
);

